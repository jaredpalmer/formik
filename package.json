{
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "test": "lerna run --stream test",
    "start": "lerna run --parallel start",
    "prepare": "lerna run --parallel build",
    "build": "lerna run --parallel build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.13.1",
    "rimraf": "^2.6.3"
  }
}