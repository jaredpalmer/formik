// Temporary fallback for next/font during build in restricted network environment
// This will be removed after successful upgrade
export const Inter = (config: any) => ({
  className: 'font-sans',
});
